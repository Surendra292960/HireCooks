/*
package com.test.sample.hirecooks;
import androidx.appcompat.app.AppCompatActivity;
import androidx.appcompat.widget.AppCompatButton;
import android.os.Bundle;
import android.view.View;
import android.widget.ImageView;
import android.widget.TextView;
import com.google.android.material.textfield.TextInputEditText;
import com.test.sample.hirecooks.Utils.ProgressBarUtil;

public class EmailAndPhoneVarification extends AppCompatActivity {
    private static final String TAG = "EmailAndPhoneVarification";
    private TextInputEditText editText;
    private AppCompatButton verifyBtn;
    private TextView textView;
    private ImageView imageView;
    private ProgressBarUtil progressBarUtil;

    @Override
    protected void onCreate(Bundle savedInstanceState) {
        super.onCreate(savedInstanceState);
        setContentView(R.layout.activity_email_and_phone_varification);
        initializeView();
    }

    private void initializeView() {
        progressBarUtil = new ProgressBarUtil(this);
        editText = findViewById(R.id.editTextEmail);
        imageView = findViewById(R.id.imageView);
        //textView = findViewById(R.id.verified);
        verifyBtn = findViewById(R.id.verifyBtn);
        imageView.setOnClickListener(new View.OnClickListener() {
            @Override
            public void onClick(View v) {

            }
        });

        verifyBtn.setOnClickListener(new View.OnClickListener() {
            @Override
            public void onClick(View v) {

            }
        });
    }
}
*/
